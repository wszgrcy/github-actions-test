name: CI

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: init
        run: |
          git config --global user.name "${GITHUB_ACTOR}"
          git config --global user.email "${GITHUB_ACTOR}@gmail.com"
      - name: git
        run: |
          git clone https://github.com/${GITHUB_REPOSITORY}.git
          cd github-actions-test
      # - uses: actions/checkout@v2-beta
      #   with:
      #     token: ${{ secrets.ACCESS_TOKEN }}
      - uses: actions/setup-node@v1
        with:
          node-version: 12.x
      - name: build
        env:
          REPOSITORY_PATH: https://${{secrets.ACCESS_TOKEN}}@github.com/

        run: |
          # git pull ${REPOSITORY_PATH}${GITHUB_REPOSITORY}.git
          # echo $GITHUB_REPOSITORY
          echo ${REPOSITORY_PATH}${GITHUB_REPOSITORY}.git
          set
          npm i
          # npm run build
          ls -l

          git config --system --list
          git config --global  --list
          git config --local  --list
          touch test123
          git add -A
          git status
          git commit -m 'test'
          ls -l

          git push --force ${REPOSITORY_PATH}${GITHUB_REPOSITORY}.git master
          # git branch -d gh-pages
          # git branch gh-pages
          # git checkout gh-pages
          ls -l
          # git pull
